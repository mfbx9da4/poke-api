import { suite } from "uvu";
import * as assert from "uvu/assert";

// import { PokeSDK } from "../../speakeasy-generator/src";

// const it = suite("speakeasy-generator");

// it("should get by id", async () => {
//   const sdk = new PokeSDK();
//   const result = await sdk.pokemon.pokemonRead(1);
//   assert.ok(result.pokemon);
//   assert.is(result.pokemon.id, 1);
//   assert.is(result.statusCode, 200);
// });

// it("should handle not found", async () => {
//   const sdk = new PokeSDK();
//   const result = await sdk.pokemon.pokemonRead(-1);
//   assert.is(result.pokemon, undefined, "pokemon should be null");
//   assert.is(result.statusCode, 404, "statusCode should be 404");
// });

// it.run();
